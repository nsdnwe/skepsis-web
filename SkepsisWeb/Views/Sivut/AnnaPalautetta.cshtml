@model SkepsisWeb.Models.Feedback

@{
    ViewBag.Title = "Anna palautetta";
}
<div class="row">
    <div class="col-md-12 text-left data">
        <h1>Palaute</h1>
        <p>Voit antaa palautetta Skepsikselle tämän lomakkeen kautta. </p>
        <p>Voit lähettää palautetta myös sähköpostilla yhdistyksen <a href="/Yhteystiedot">sihteerille</a>.</p>

        <p></p>
        @if (!string.IsNullOrEmpty(Model.CompletedMessage)) {
            <div class="alert alert-success" role="alert">
                @Model.CompletedMessage
            </div>
        } else {
            @Html.Partial("_ValidationSummary")
            <form id="my-form" class="bs-example form-horizontal" method="post" enctype="multipart/form-data">
                <fieldset>
                    <div class="form-group">
                        <label for="inputUserName" class="col-lg-2 control-label">Nimi</label>
                        <div class="col-lg-10">
                            @Html.TextBoxFor(model => model.Name, new { @class = "form-control" })
                        </div>
                    </div>
                    <div class="form-group">
                        <label for="inputUserName" class="col-lg-2 control-label">Sähköpostiosoite</label>
                        <div class="col-lg-10">
                            @Html.TextBoxFor(model => model.Email, new { @class = "form-control" })
                        </div>
                    </div>
                    <div class="form-group">
                        <label for="inputUserName" class="col-lg-2 control-label">Puhelinnumero</label>
                        <div class="col-lg-10">
                            @Html.TextBoxFor(model => model.Phone, new { @class = "form-control" })
                        </div>
                    </div>
                    <div class="form-group">
                        <label for="inputUserName" class="col-lg-2 control-label">Palaute</label>
                        <div class="col-lg-10">
                            @Html.TextAreaFor(model => model.Info, new { @class = "form-control", @id = "textArea", @style = "height:100px" })
                        </div>
                    </div>
                    <div style="width:100%; text-align:right">
                        <button class="btn btn-primary btn-normal g-recaptcha" name="button" value="ok" data-sitekey="6Ldy7KEUAAAAACv56F1nWiDsncJiUHHddIsh_Ztr" data-callback='onSubmit'>Lähetä</button>
                    </div>
                </fieldset>
            </form>
        }
    </div>
</div>
<script>
       function onSubmit(token) {
           document.getElementById("my-form").submit();
       }
</script>
